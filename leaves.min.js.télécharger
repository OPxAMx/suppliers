var locked=false;window.onscroll=function(){requestAnimationFrame(()=>{if(!locked&&innerWidth>=991){locked=true;let limit=document.body.scrollHeight-document.body.offsetHeight;let angle=parseInt(scrollY*38/limit);document.querySelector(":root").style.setProperty("--rotation",angle);locked=false}})};